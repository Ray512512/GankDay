<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ray.gank">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GankApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainDrawerActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.MeiZhiActivity"
            android:launchMode="singleTask"
            android:theme="@style/MeizhiTheme"/>
        <activity android:name=".ui.activity.WebActivity"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme2"/>
        <activity android:name="com.ray.library.view.view.photoview.ImageGalleryActivity"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme2"/>
    </application>

</manifest>